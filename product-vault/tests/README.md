# Acceptance Tests

Each approved requirement must have test scenarios written before implementation. Tests prove user behaviour, permissions, validations and side effects—not merely that a screen renders.

Use `Given / When / Then` language and link the test page from its requirement.
